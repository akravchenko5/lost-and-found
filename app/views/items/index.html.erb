<div class="show-page-wrap">

  <div class="show-container-list">
    <h2><span class="green"><%= @items.count %></span> results</h2>

    <div class="show-card-wrap">
      <%# ip = Ip::Lookup.server_whatismyipaddress %>
      <%# location = Geocoder.search(ip).first.coordinates %>
    <%= render 'shared/show_card', items: @items, location: @location %>

    </div>
  </div>

</div>
